<h2>about</h2>
<div>
  <h2>!Hola {{user.name}}</h2>
  {{#if isAdmin}}
  {{#each food}}
  <div>
    <p>{{this.name}}</p>
    <p>{{this.price}}</p>
  </div>
    {{/each}}
    {{else}}
    <p>No tienes permisos</p>
    {{/if}}

  {{#each userPicked}}
  <div>
    <p>{{this.nombre}}</p>
    <p>{{this.apellido}}</p>
    <p>{{this.email}}</p>



   {{/each}}
  </div>
   


</div>